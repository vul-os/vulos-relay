# JS build artifacts
node_modules/
dist-lib/
/dist/

# Go build artifacts (sovereign reverse tunnel)
/vulos-relayd
/vulos-relay-agent

# Databases
*.db

# Self-host secrets generated by scripts/install.sh — NEVER commit these.
/.env
/grants.json
/revoked.json

# macOS + IDE
.DS_Store
.claude

# Screenshots — generated by `npm run screenshots`
# Commit the outputs (docs/screenshots/*.png) so the README renders on GitHub.
# Ignore only the Playwright cache that the scripts/ install pulls in.
scripts/node_modules/
scripts/package-lock.json

# Rust build output
/target
