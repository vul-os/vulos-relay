node_modules/
dist-lib/
*.tgz

# Built by e2e/harness — the built-library consumer used by the E2E boot guard
dist-harness/

# Playwright (E2E boot guard) output
test-results/
playwright-report/
blob-report/
